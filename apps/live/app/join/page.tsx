"use client";
import { useState } from "react";
export default function JoinPage(){const [firstName,setFirstName]=useState(""),[done,setDone]=useState(false),[error,setError]=useState(""); async function submit(e:React.FormEvent){e.preventDefault();const r=await fetch("/api/join",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({firstName})});if(r.ok)setDone(true);else setError("Please try again.")} return <main style={{maxWidth:520,margin:"10vh auto",padding:24}}>{done?<><h1>Welcome to Retroverse.</h1><p>Your member profile is ready. You can register a pass later.</p></>:<><h1>Join Retroverse</h1><form onSubmit={submit}><label>First Name<input required value={firstName} onChange={e=>setFirstName(e.target.value)}/></label><button type="submit">Join Retroverse</button>{error&&<p>{error}</p>}</form></>}</main>}
